[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "pycr"
version = "0.1.0"
requires-python = ">=3.9"
readme = "README.md"
dependencies = [
    "numpy>=2.0.2",
    "sympy>=1.14.0",
]

# jax or some JIT like numba
# CUDA?
optional-dependencies = {  }
