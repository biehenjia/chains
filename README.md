# chains
chains of recurrences engine in python

# installation

## manual


# examples
```python
from pycr import chainify

cr, symbol_table = chainify("x**2+sin(x)+4")

print(cr)
print(symbol_table)
```